\ProvidesPackage{preamble}

\usepackage[utf8]{inputenc}

% MATHS
\usepackage{amsmath}
\usepackage{amssymb}

% PAGE MARGINS
\usepackage[a4paper, textwidth=6.5in, top=1in, bottom=1in]{geometry}

% ADD BIBLIOGRAPHY
\usepackage[sorting=none]{biblatex}
\addbibresource{bibliography.bib}

% LINK CONTENTS AND BIBLIOGRAPHY TO DOCUMENT
\usepackage{xcolor}
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linktoc=all,
    linkcolor=black,
    citecolor=black
}

% CHAPTER STYLING
\usepackage{titlesec, blindtext, color}
\definecolor{gray75}{gray}{0.75}
\newcommand{\hsp}{\hspace{20pt}}
\titleformat{\chapter}[hang]{\Huge\bfseries}
{\thechapter\hsp\textcolor{gray75}
{$|$}\hsp}{0pt}{\Huge\bfseries}
\titlespacing{\chapter}{0pt}{0pt}{40pt}

% PARAGRAPH STYLING
\newcommand{\setparagraphstyling}{
    \setlength{\parindent}{0em}
    \setlength{\parskip}{1em}
}

% HEADER / FOOTER STYLING
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\rhead{Joshua Smart}
\lhead{\leftmark}
\cfoot{\thepage}

\fancypagestyle{plain}{
    \fancyhead{}
    \rhead{Joshua Smart}
    \lhead{\leftmark}
    \cfoot{\thepage}
}
\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\footrulewidth}{0.5pt}

% IMAGES
\usepackage{wrapfig}
\usepackage{graphicx}
\graphicspath{{./assets/}}

% LAYOUT
\usepackage{multicol}
\newcommand{\prosandcons}[2]{
    \paragraph{Pros}
    \begin{itemize}
        #1
    \end{itemize}
    \paragraph{Cons}
    \begin{itemize}
        #2
    \end{itemize}
}

% CODE
\usepackage[outputdir=build]{minted}

\newcommand{\mintTS}[1]{\mintinline{TypeScript}{#1}}

\definecolor{typescolor}{HTML}{56b6c2}

% Type keywords:
% number, string, Map, Model, RoadNetwork, CubicBezier, Vector2, void, Simulation, Edge, Vertex, StateMachine

\newcommand{\inlinecode}[1]{\colorbox{backcolour}{\lstinline!#1!}}
\usepackage{algpseudocode}
\usepackage{algorithm}

% FIGURES
\usepackage[justification=centering]{caption}

% UML CLASS DIAGRAM
\newcommand{\umldiagram}[3]{
    \begin{table}[ht]
        \centering
        \begin{tabular}{|l|}
            \hline
            \textbf{#1} \\
            \hline
            #2 \\
            \hline
            #3 \\
            \hline
        \end{tabular}
        \caption{#1 class diagram}
        \label{UML:#1}
    \end{table}
}
